name: Deploy to Lightsail
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger self-update
        run: |
          curl -s -X POST http://13.206.91.12:5000/api/self_update \
            -H "Content-Type: application/json" \
            -d '{"token":"alphabot2025deploy"}' || echo "Bot will self-update on next restart"